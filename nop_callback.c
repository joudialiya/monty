#include "monty.h"


/**
 * nop_callback - callback
 * @head: ptr to the head of the list
 * @line_number: line_number
 */
void nop_callback(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
}
